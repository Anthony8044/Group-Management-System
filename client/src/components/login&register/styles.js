import { makeStyles } from '@mui/styles';



export default makeStyles((theme) => ({
    infoContent: {
        margin: theme.spacing(2),
    },
    paperContainer: {
        height: '100vh',
        width: '100vh'
    },
    mainLogo: {
        marginBottom: '80px',
        marginTop: '140px'
    }
}));